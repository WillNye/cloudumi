import { Meta } from '@storybook/addon-docs';
import { icons, VendorIconDemo } from './VendorIconDemo';

<Meta title="General/Vendor Icons" />

## Vendor

<style>
  {`
  .grid {
    display: grid;
    grid-gap: 15px;
    grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
    justify-items: stretch;
  }
`}
</style>

<div className="grid">
  {icons.map(key => (
    <VendorIconDemo key={key.title} {...key} />
  ))}
</div>
